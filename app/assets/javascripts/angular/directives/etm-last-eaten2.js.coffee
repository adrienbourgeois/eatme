directives = angular.module('directives')

# directives.controller 'EtmLastEaten2Ctrl', ['$scope',($scope) ->
#   $scope.messageFromController = "hello from controller"
#   $scope.coordinates = 0.0

#   if navigator.geolocation
#     navigator.geolocation.getCurrentPosition (position) ->
#       $scope.coordinates = position.coords.latitude
#       console.log '$scope.coordinates'
#       console.log $scope.coordinates
#       $scope.$apply()
# ]

# directives.directive 'etmLastEaten2', ->
#   return {
#     restrict: "A"
#     controller: 'EtmLastEaten2Ctrl'
#     templateUrl: '/assets/etm_last_eaten.html'
#   }
